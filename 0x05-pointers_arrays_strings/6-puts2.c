#include <stdio.h>
#include "main.h"

/**
*function  that prints every character in s string,
*starting with the first character 
*/
void puts2(char *str)
{
  int x = 0;

  while(str[x] = str[1])
 {
      _putchar(str[x]);
      x++;
}
